# Asset Manifest

This directory stores metadata and delivery manifests, not large source binaries.

Each approved asset record should include: stable ID, display name, purpose, linked requirement, source storage URI, preview URI, rights status, version, approver, and export target.
