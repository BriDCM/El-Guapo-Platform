/**
 * Placeholder for the Unity project-scoped exchange contract.
 * Unity editor integration begins after a managed project is registered.
 */
export type UnityProjectHandshake = {
  projectId: string;
  unityVersion: string;
};
