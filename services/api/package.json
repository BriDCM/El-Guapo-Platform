{
  "name": "@el-guapo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "echo 'No API tests configured yet; see task acceptance record.'",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@el-guapo/data-store": "0.1.0",
    "@el-guapo/shared-schema": "0.1.0",
    "@fastify/cors": "^10.0.1",
    "fastify": "^5.2.1"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
